import React, { Component } from 'react';
import { View, Text, ImageBackground } from 'react-native';

class RegisterForm extends Component {
  render(){
    return (
      <ImageBackground source={require('../../../assets/court2-bg.jpg')}>
        <View>

        </View>
      </ImageBackground>
    );
  }
}

export default RegisterForm;